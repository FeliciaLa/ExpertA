{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --upgrade pip && pip install -r requirements-railway.txt && pip install gunicorn djangorestframework-simplejwt==5.3.1 PyJWT==2.8.0"
  },
  "deploy": {
    "startCommand": "cd /backend && gunicorn expert_system.wsgi:application --log-file -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 