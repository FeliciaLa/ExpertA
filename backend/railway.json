{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --upgrade pip && pip install -r backend/requirements-railway.txt"
  },
  "deploy": {
    "startCommand": "cd backend && python -c 'from expert_system.jwt_helper import ensure_jwt_available; ensure_jwt_available()' && gunicorn expert_system.wsgi:application --log-file -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 