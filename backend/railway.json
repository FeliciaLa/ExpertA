{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install djangorestframework-simplejwt==5.3.1 PyJWT==2.8.0 && pip install --upgrade pip && pip install -r backend/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn expert_system.wsgi:application --log-file -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 