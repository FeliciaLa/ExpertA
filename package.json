{
  "name": "experta-backend",
  "version": "1.0.0",
  "description": "Django backend API for ExpertA with supporting Node.js utilities",
  "main": "server.js",
  "scripts": {
    "start": "echo 'Using Dockerfile for primary deployment - Node.js server is supplementary only'",
    "start:node": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
